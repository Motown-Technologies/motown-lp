---
const { as = "h3", class: className = "" } = Astro.props;
const Tag = as;
const baseClasses =
    "font-display text-2xl md:text-3xl font-bold tracking-tight leading-[1.05] mb-3";
---

<Tag class={`${baseClasses} ${className}`.trim()}>
    <slot />
</Tag>

---
const { class: className = "" } = Astro.props;

const baseClasses =
    "w-12 h-12 bg-[#2563eb]/10 rounded-lg flex items-center justify-center mb-6 transition-colors group-hover:bg-[#2563eb]/10";
---

<div class={`${baseClasses} ${className}`.trim()}>
    <slot />
</div>
